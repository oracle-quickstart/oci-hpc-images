# NOTE: This is an automatically-generated file!  (generated by the
# Open MPI/SHMEM RPM).  Any changes made here will be lost if the RPM is
# uninstalled or upgraded.

# PATH
if test -z "`echo $PATH | grep {{ openmpi.files[0].path }}/bin`"; then
    PATH=/usr/mpi/gcc/{{ openmpi.files[0].path }}/bin:${PATH}
    export PATH
fi

# LD_LIBRARY_PATH
if test -z "`echo $LD_LIBRARY_PATH | grep {{ openmpi.files[0].path }}/lib`"; then
    LD_LIBRARY_PATH=/usr/mpi/gcc/{{ openmpi.files[0].path }}/lib${LD_LIBRARY_PATH:+:}${LD_LIBRARY_PATH}
    export LD_LIBRARY_PATH
fi

# MANPATH
if test -z "`echo $MANPATH | grep {{ openmpi.files[0].path }}/share/man`"; then
    MANPATH=/usr/mpi/gcc/{{ openmpi.files[0].path }}/share/man:${MANPATH}
    export MANPATH
fi

# MPI_ROOT
MPI_ROOT={{ openmpi.files[0].path }}
export MPI_ROOT